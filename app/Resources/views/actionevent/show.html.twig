{% extends 'base.html.twig' %}

{% block body %}
    <h1>ActionEvent</h1>

    <table>
        <tbody>
            <tr>
                <th>Idevent</th>
                <td>{{ actionEvent.idEvent }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('actionevent_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('actionevent_edit', { 'id': actionEvent.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
