{% extends 'base.html.twig' %}

{% block body %}
    <h1>BidClient</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bidClient.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bidclient_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bidclient_edit', { 'id': bidClient.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
