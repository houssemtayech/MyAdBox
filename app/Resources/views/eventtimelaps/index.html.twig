{% extends 'base.html.twig' %}

{% block body %}
    <h1>EventTimelaps list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for eventTimelap in eventTimelaps %}
            <tr>
                <td><a href="{{ path('eventtimelaps_show', { 'id': eventTimelap.id }) }}">{{ eventTimelap.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('eventtimelaps_show', { 'id': eventTimelap.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('eventtimelaps_edit', { 'id': eventTimelap.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('eventtimelaps_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
