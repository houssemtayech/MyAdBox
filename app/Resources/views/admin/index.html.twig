{% extends 'base.html.twig' %}

{% block body %}
    <h1>Admin list</h1>

    <table>
        <thead>
            <tr>
                <th>Groupe</th>
                <th>Cin</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for admin in admins %}
            <tr>
                <td><a href="{{ path('admin_show', { 'id': admin.id }) }}">{{ admin.groupe }}</a></td>
                <td>{{ admin.cin }}</td>
                <td>{{ admin.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_show', { 'id': admin.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_edit', { 'id': admin.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
