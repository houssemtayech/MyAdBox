{% extends 'base.html.twig' %}

{% block body %}
    <h1>ActionTimelaps</h1>

    <table>
        <tbody>
            <tr>
                <th>Idtimelaps</th>
                <td>{{ actionTimelap.idTimelaps }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('actiontimelaps_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('actiontimelaps_edit', { 'id': actionTimelap.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
